import {AppShell} from '@/components/layout/AppShell';
import {ValueBetsView} from '@/components/value-bets/ValueBetsView';

export default function ValueBetsPage() {
  return (
    <AppShell>
      <ValueBetsView />
    </AppShell>
  );
}
