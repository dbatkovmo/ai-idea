import {AppShell} from '@/components/layout/AppShell';
import {ModelStatsView} from '@/components/model-stats/ModelStatsView';

export default function ModelStatsPage() {
  return (
    <AppShell>
      <ModelStatsView />
    </AppShell>
  );
}
